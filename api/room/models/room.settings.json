{
  "kind": "collectionType",
  "collectionName": "rooms",
  "info": {
    "name": "Room"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "name": {
      "type": "string"
    },
    "bookings": {
      "collection": "booking",
      "via": "room"
    },
    "roomType": {
      "type": "enumeration",
      "enum": [
        "meetingRoom",
        "callRoom",
        "cozyLouge"
      ]
    }
  }
}
