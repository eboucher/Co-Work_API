{
  "routes": [
    {
      "method": "GET",
      "path": "/meeting-rooms",
      "handler": "meeting-room.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/meeting-rooms/count",
      "handler": "meeting-room.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/meeting-rooms/:id",
      "handler": "meeting-room.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/meeting-rooms",
      "handler": "meeting-room.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/meeting-rooms/:id",
      "handler": "meeting-room.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/meeting-rooms/:id",
      "handler": "meeting-room.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
